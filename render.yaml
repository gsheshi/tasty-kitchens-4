services:
  - type: web
    name: my-app
    env: node
    buildCommand: |
      corepack enable
      corepack prepare pnpm@8.15.4 --activate
      pnpm install
      pnpm run build
    startCommand: pnpm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
